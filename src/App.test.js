import { render, screen } from '@testing-library/react';
import App from './App';

test('renders learn react link', () => {
  render(<App />);
});

test('renders learn react link', () => {
  render(<App />);
});

// ye test script edit kia hai isme jo test hota tha wo element mene 
// app.js se nikal dia tha isliye 
